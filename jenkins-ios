
pipeline {
    agent any
    stages {
        stage ('Invoke_pipeline') {
            steps {	    
                build job: 'Quinnox-ios', parameters: ( [
		string(name: 'DEVICE_TYPE', value: "iOS")
                ] )
            }
        }

        stage('End') {
            steps {
                echo 'ls'
            }
       }        
    }
}
